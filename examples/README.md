# Examples

Sample FITS captures so you can try the standalone viewers without an
FMCOMMS5:

- `test.fits` — a recorded UV / dirty-image FITS suitable for both viewers.

```bash
ori-uv    examples/test.fits
ori-clean examples/test.fits
```

FITS files in this folder are intentionally **not** ignored by
`.gitignore`.
